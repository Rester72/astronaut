using UnityEngine;

public class DetectionZone : MonoBehaviour
{
    public BirdEnemySimple bird;

    private void OnTriggerEnter2D(Collider2D other)
    {
        if (other.CompareTag("Player"))
        {
            bird.SetPlayerInRange(true, other.transform);
        }
    }
}